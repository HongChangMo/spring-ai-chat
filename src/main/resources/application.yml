spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o

chat:
  memory:
    window-size: 10

rag:
  document-paths: docs/faq.txt,docs/refund-policy.txt

